import { CustomUserAgentDetails, InAppMessagingAction } from '@aws-amplify/core';
export declare const getUserAgentValue: (action: InAppMessagingAction, customUserAgentDetails?: CustomUserAgentDetails) => string;
